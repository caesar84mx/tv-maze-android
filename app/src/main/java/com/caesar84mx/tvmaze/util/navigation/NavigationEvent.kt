package com.caesar84mx.tvmaze.util.navigation

/**
 * A marker interface representing a navigation event within the application.
 * Subtypes include [Destination] and [SystemNavigationEvent].
 */
interface NavigationEvent
